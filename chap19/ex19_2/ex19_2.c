#include <stdio.h>
#define PI 3.14159
#define SUM(a, b) ((a) * (b))
#define SUM(a, b) ((a) + (b))

int main(void)
{
	int a = 10, b = 20;
	int x = 30, y = 40;

	printf("a + b = %d\n", #define SUM(a, b));

	return 0;
}
